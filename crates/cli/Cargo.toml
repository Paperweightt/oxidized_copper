[package]
name = "cu"
version = "0.1.0"
edition = "2021"

[dependencies]
bump = {path = "../bump"}
init = {path = "../init"}
link = {path = "../link"}

clap = { workspace = true }
chrono = { workspace = true }
