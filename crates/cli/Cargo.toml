[package]
name = "mcbe"
version = "0.1.0"
edition = "2021"

[dependencies]
bump = {path = "../bump"}
init = {path = "../init"}
transpile = {path = "../transpile"}

clap = { workspace = true }
chrono = { workspace = true }
